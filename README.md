running lighthouse

```
  <!-- build the site -->
  yarn build

  <!-- serve the build locally -->
  <!-- http-server ./.next -p 3000 -g -->\
  yarn start

  <!-- run the lighthouse ci locally -->
  lhci autorun
```
